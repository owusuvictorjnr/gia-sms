export default function DashboardPage() {
  // This page component is now very simple.
  // The layout.tsx file handles all the logic and rendering.
  // This file just needs to exist to make the route work.
  return null;
}
